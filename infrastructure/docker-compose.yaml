version: "3.9"

networks:
  alicante:
    external: true 

services:
  # ----------------------------------------
  # Eureka Server
  # ----------------------------------------
  eureka-server:
    image: eclipse-temurin:21-jdk  # <-- Use Java 21 to match your build
    build:
      context: ./eureka-server
      dockerfile: Dockerfile
    container_name: eureka-server
    hostname: eureka-server
    networks:
      alicante:
        ipv4_address: 192.168.100.50
    ports:
      - "8761:8761"
    environment:
      - SPRING_PROFILES_ACTIVE=docker
      - EUREKA_INSTANCE_HOSTNAME=eureka-server
      - EUREKA_CLIENT_REGISTER_WITH_EUREKA=false
      - EUREKA_CLIENT_FETCH_REGISTRY=false
      - SERVER_PORT=8761
    # deploy:
    #   replicas: 1
    # healthcheck:
    #   test: ["CMD-SHELL", "curl -f http://localhost:8761/actuator/health || exit 1"]
    #   interval: 10s
    #   timeout: 5s
    #   retries: 5
    #   start_period: 20s

  # ----------------------------------------
  # API Gateway
  # ----------------------------------------
  # api-gateway:
  #   image: eclipse-temurin:21-jdk
  #   build:
  #     context: ./api_gateway # Assumes your API Gateway code is in a folder named 'api-gateway'
  #     dockerfile: Dockerfile
  #   container_name: api-gateway
  #   hostname: api-gateway
  #   networks:
  #     alicante:
  #       ipv4_address: 192.168.100.51 # Assigning a different IP in the same network
  #   ports:
  #     - "5274:8080"
  #   environment:
  #     - SPRING_APPLICATION_NAME=api-gateway
  #     - SERVER_PORT=8080
  #     # This environment variable overrides the one in application.yml to ensure
  #     # it uses the Docker network address for Eureka.
  #     - EUREKA_CLIENT_SERVICEURL_DEFAULTZONE=http://192.168.100.50:8761/eureka/
  #   depends_on:
  #     eureka-server:
  #       condition: service_healthy # Waits for Eureka server to be healthy before starting
  #   deploy:
  #     replicas: 1
  #   healthcheck:
  #     test: ["CMD-SHELL", "curl -f http://localhost:8080/actuator/health || exit 1"]
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5
  #     start_period: 30s